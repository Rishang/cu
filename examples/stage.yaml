metadata:
  name: my-service
  creationTimestamp: "2024-06-15T10:00:00Z"

status:
  ready: false
  phase: Pending

spec:
  replicas: 1
  image: myapp:v2.1.0
  resources:
    limits:
      cpu: "200m"
      memory: "128Mi"
    requests:
      cpu: "50m"
      memory: "64Mi"
  env:
    - name: LOG_LEVEL
      value: debug
    - name: DB_HOST
      value: stage-db.internal

data:
  password: stagepw123
